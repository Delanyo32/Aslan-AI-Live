// Migration is now handled by drizzle-kit.
//
// Commands:
//   bun run db:generate   — generate a new migration from schema changes
//   bun run db:migrate    — apply pending migrations to the database
//   bun run db:push       — push schema directly to DB (dev only, no migration files)
//   bun run db:studio     — open Drizzle Studio (visual DB browser)
//
// Migration files live in: src/lib/server/db/migrations/drizzle/
// Schema definitions live in: src/lib/server/db/schema/
